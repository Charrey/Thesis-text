package data.graph;

public enum Label {
    PIN, COMPONENT, PORT, MUX, IN, SELECT, OUT, LUT, SYNC_RESET, ASYNC_RESET, SYNC_SET, GATE, OPTION, SWITCH, FLOW_TO, FLOW_FROM, CLOCK, EXTRA, REMOVE, ASYNC_SET, CLOCK_ENABLE, RISING_EDGE, FALLING_EDGE, CLOCK_FRAME, REGISTER;
}
